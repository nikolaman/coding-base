# Секреты — только локально, никогда в git
# cp .env.example .env

# --- Database ---
DB_USER=app
DB_PASSWORD=changeme
DB_NAME=app
DB_HOST=localhost
DB_PORT=5432

# --- Deploy (CI Variables в GitHub/GitLab) ---
# MIGRATION_CMD=alembic upgrade head
# DEPLOY_STRATEGY=rolling
# PHP_SMOKE_URL=http://127.0.0.1:8081/health
# PYTHON_SMOKE_URL=http://127.0.0.1:8082/health

# --- Feature flags ---
# FEATURE_NEW_CHECKOUT=false

# --- Notifications (GitHub Secrets) ---
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# TEAMS_WEBHOOK_URL=https://...
